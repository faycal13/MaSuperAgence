{% extends 'base.html.twig' %}

{% block title 'Ma Super Agence' %}

{% block body %}
    <div class="jumbotron text-center">
        <h1>Ma Super Agence</h1>
        <p>Lorem ipsum dolor sit amet, consectetur adipisicing elit. Accusamus atque autem blanditiis, debitis ea expedita inventore maiores minus natus nisi obcaecati quam suscipit tenetur ullam vitae? Dicta laborum nisi quas.
        </p>
    </div>
    <div class="container">
        <h2>Les Derniers Biens</h2>
        <div class="row flex">
            {% for property in properties  %}
                <div class="col-3">
                    <div class="card">
                        <div class="card-body">
                            <h5 class="card-title">
                                <a href="{{ path('property.show', {id: property.id, slug: property.slug}) }}">{{ property.title }}</a>
                            </h5>
                            <p class="card-text">{{ property.city }} ({{ property.postalCode }})</p>
                            <div class="text-danger lead">{{ property.formattedPrice }} €</div>
                        </div>
                    </div>
                </div>
            {% endfor %}
        </div>
    </div>

{% endblock %}